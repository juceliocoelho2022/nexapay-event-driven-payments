server:
  port: 8085

spring:
  application:
    name: nexapay-auth-service

  datasource:
    url: jdbc:postgresql://localhost:5439/nexapay_auth
    username: nexapay
    password: nexapay

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  file:
    name: ${NEXAPAY_LOG_DIR:logs}/nexapay-auth-service.log

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: ${NEXAPAY_TRACING_SAMPLING_PROBABILITY:1.0}
  otlp:
    tracing:
      endpoint: ${NEXAPAY_OTLP_TRACING_ENDPOINT:http://localhost:4318/v1/traces}
  prometheus:
    metrics:
      export:
        enabled: true

nexapay:
  security:
    jwt:
      issuer: https://nexapay.local/auth
      secret: ${NEXAPAY_JWT_SECRET:nexapay-local-dev-secret-2026-change-me}
      ttl-seconds: 3600
